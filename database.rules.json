{
  "rules": {
    "chats": {
      ".read": "auth != null",
      ".write": "auth != null",
      "$messageId": {
        ".validate": "newData.hasChildren(['userId', 'message', 'timestamp', 'userProfile'])"
      }
    }
  }
}